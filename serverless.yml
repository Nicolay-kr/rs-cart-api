service: serverless-nest

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs14.x

functions:
  main:
    handler: dist/main.handler
    events:
      - http:
          method: any
          path: /
      - http:
          method: any
          path: '{proxy+}'